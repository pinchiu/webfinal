## 1. Backend Data Setup

- [ ] 1.1 Create a SQL Server database initialization script (SQL) to create the `companies` table and insert all data from the current `app.js`.
- [ ] 1.2 Setup the `backend/db.php` file with PDO connection logic for SQL Server.

## 2. PHP API Development

- [ ] 2.1 Create `backend/api/get_companies.php` to fetch and return companies as JSON.
- [ ] 2.2 Test the API endpoint to ensure it returns correctly formatted JSON.